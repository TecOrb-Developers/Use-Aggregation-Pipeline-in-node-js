"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
exports.__esModule = true;
var commander_1 = require("commander");
var index_1 = __importDefault(require("../src/index"));
commander_1.program
    .option('-d, --dev', 'development run typescript program')
    .option('-b, --build', 'build typescript program');
commander_1.program.parse(process.argv);
var sourceFiles = commander_1.program.args;
if (commander_1.program.dev) {
    index_1["default"](sourceFiles);
}
else if (commander_1.program.build) {
    console.warn('build project is not supported temporarily');
    // runMain(sourceFiles);
}
else {
    index_1["default"](sourceFiles);
}
